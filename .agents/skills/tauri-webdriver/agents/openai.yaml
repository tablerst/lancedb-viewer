interface:
  display_name: "Tauri WebDriver"
  short_description: "Debug LanceDB Viewer in the real Tauri WebView."
  default_prompt: "Use $tauri-webdriver to inspect LanceDB Viewer in a real Tauri WebView and check layout at narrow and desktop widths."
